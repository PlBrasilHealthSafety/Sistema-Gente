'use client';

export default function Footer() {
  return (
    <footer className="fixed bottom-0 left-0 right-0 bg-white/85 backdrop-blur-sm border-t border-gray-300/60 py-4 px-4 z-10">
      <div className="flex justify-center items-center">
        <p className="text-xs font-medium text-[#00A298]/80 leading-tight whitespace-nowrap">
          Recuperar o tempo perdido com o que realmente importa!
        </p>
      </div>
    </footer>
  );
} 